<?php
    $_collection = $this->getTicketList();
    $helper = Mage::helper('inchoo_supportticket')
?>
<div class="page-title title-buttons">
    <h1>
        <?php echo $this->__('My Tickets') ?>
    </h1>
    <button type="button" title="<?php echo $this->__('New Ticket') ?>"
            class="button"
        onclick="window.location='<?php echo $this->getAddTicketUrl(); ?>';">
        <span>
            <span>
                <?php echo $this->__('New Ticket') ?>
            </span>
        </span>
    </button>

</div>

<div class="customer-list">
    <?php if(!$_collection->count()): ?>
    <h2><?php echo $this->__('You have no tickets.') ?></h2>
    </a>
    <?php else: ?>
        <table class="data-table" id="tickets_table" >
            <thead>
                <tr class="first last">
                    <th>Ticket #</th>
                    <th>Subject</th>
                    <th><span class="nobr">Status</span></th>
                    <th><span class="nobr">View</span></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($_collection as $ticket): ?>
                    <tr>
                        <td>
                            <?php echo $ticket->getTicketId(); ?>
                        </td>
                        <td>
                            <?php echo $ticket->getSubject(); ?>
                        </td>
                        <td>
                            <?php if ($ticket->getStatus() == 0): ?>
                                Closed
                            <?php else: ?>
                                Open
                            <?php endif ?>
                        </td>
                        <td>
                            <a href="<?php echo $this->getUrl('tickets/support/view', array('_query' => array('ticket_id' => $ticket->getTicketId()))) ?>">
                                <?php echo $this->__('View') ?>
                            </a>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    <?php endif; ?>
</div>
